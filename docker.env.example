# Copy to .env in the project root (or merge these keys into your existing .env).
# Docker Compose reads .env for ${POSTGRES_PASSWORD} and ${DB_PASSWORD} substitution.
# Use the same value for both so the app can connect to Postgres.

POSTGRES_PASSWORD=change-me-to-a-strong-secret
DB_PASSWORD=change-me-to-a-strong-secret
